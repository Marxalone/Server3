services:
  - type: web
    name: whatsapp-analytics
    env: node
    build:
      node:
        version: ">=20.0.0"
    envVars:
      - key: PORT
        value: "10000"
      - key: NODE_ENV
        value: "production"